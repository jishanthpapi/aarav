// MERGED — see LBMSolver.ts. The force-integration resources and dispatch
// order described here (originally a standalone "additions" file in the
// source transcript, never spliced into the real class) are now part of
// LBMSolver's init()/step()/readForces(). Kept only for provenance.
export {};

// STUB — Workstation.tsx imports this, but no PartLibrary implementation
// exists anywhere in the source transcript this project was extracted from.
// Part/slider wiring is explicitly listed as "outstanding" in HANDOFF-SOLVER.md
// section 4.5. This placeholder exists only so the module graph resolves.
export default function PartLibrary() {
  return (
    <div className="p-4 text-[11px] text-white/40">
      Part library — not yet implemented. See HANDOFF-SOLVER.md §4.5.
    </div>
  );
}
